# --- Terraform ---
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
*.tfvars
!*.example.tfvars
!.terraform.lock.hcl

# Terraform backups (keep only current state)
*.tfstate.backup.*
main.tf.backup.*

# Temporary deployment files
deployment-status.txt
terraform.tfvars

# Docker build context exclusions
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/

# Local environment files
.env.local
.env.*.local

# TTS large artifacts
June/services/june-tts.zip
June/services/june-tts/checkpoints_v2.zip
June/services/june-tts/checkpoints_v2/
*.pt
*.pth
*.safetensors
*.onnx
*.ckpt

# ============================================
# TAILSCALE INTEGRATION
# ============================================

# Tailscale secrets and credentials
k8s/tailscale/tailscale-secret.yaml
.env.tailscale
june-gpu-multi-secrets.yaml

# Tailscale device state (if running locally)
tailscaled.state
*.ts-cookie

# Deployment logs and temporary files
scripts/*.log
deployment-*.log