# Auth
FIREBASE_PROJECT_ID=main-buffer-469817-v7

# Downstream services
STT_WS_URL=wss://june-stt-359243954.us-central1.run.app/ws
TTS_URL=https://june-tts-359243954.us-central1.run.app/tts

# Audio defaults
DEFAULT_LOCALE=en-US
DEFAULT_STT_RATE=16000
DEFAULT_STT_ENCODING=pcm16      # client → orchestrator → STT
DEFAULT_TTS_ENCODING=MP3        # aligns with RN client fetch

# Optional shared secret for s2s (only if NOT using user ID tokens)
INTERNAL_SHARED_SECRET=change-me
