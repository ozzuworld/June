apiVersion: v1
kind: Service
metadata:
  name: livekit-backend
  namespace: {{ .Release.Namespace }}
  labels:
    app: livekit-backend
spec:
  type: ExternalName
  externalName: livekit-livekit-server.june-services.svc.cluster.local  # ← CHANGED from .media.
  ports:
  - name: http
    port: 7880
    targetPort: 7880