apiVersion: v1
kind: Service
metadata:
  name: livekit-proxy
  namespace: {{ .Release.Namespace }}
  labels:
    app: livekit-proxy
spec:
  type: ClusterIP
  selector:
    app: livekit-proxy
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
