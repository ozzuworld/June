# June STT Service - Whisper-Streaming Real-Time Implementation
# Migrating from WhisperX (batch) to whisper-streaming (real-time)

# Web Framework
fastapi==0.104.1
uvicorn==0.24.0
python-multipart

# LiveKit Integration
livekit==0.11.1

# Whisper-Streaming Backend (faster-whisper based)
faster-whisper>=1.0.3
librosa>=0.10.0
soundfile>=0.12.1

# Voice Activity Detection (Required for whisper-streaming VAC)
torch>=2.0.0
torchaudio>=2.0.0

# Audio Processing
scipy>=1.11.0
numpy>=1.24.0

# Service Integration
httpx>=0.25.0

# Note: whisper-streaming is installed directly from GitHub
# See Dockerfile for: pip install git+https://github.com/ufal/whisper_streaming
