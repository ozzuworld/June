# June STT Service - WhisperX Enhanced Dependencies (Auto-resolve)
# Let pip automatically resolve compatible versions

# Web Framework
fastapi==0.104.1
uvicorn==0.24.0
python-multipart

# LiveKit Integration
livekit==0.11.1

# WhisperX - Latest version, let pip resolve numpy dependency
whisperx==3.7.4

# Core Dependencies (flexible versions for auto-resolution)
transformers>=4.30.0
faster-whisper>=1.0.0
pyannote.audio>=3.1.0

# Audio Processing (let pip resolve compatible versions)
soundfile>=0.12.1
scipy>=1.11.0
librosa>=0.10.0
# Remove explicit numpy constraint - let whisperx determine compatible version

# Service Integration
httpx>=0.25.0