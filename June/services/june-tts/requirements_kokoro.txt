# Kokoro TTS Ultra-Low Latency Dependencies
# For sub-100ms TTS performance in voice chat applications

# Core Kokoro dependencies
kokoro-onnx==0.1.2           # Main Kokoro inference engine
onnxruntime-gpu==1.16.3      # ONNX runtime with GPU acceleration
# Alternative for CPU-only: onnxruntime==1.16.3

# Audio processing (existing dependencies - keep)
numpy==1.24.3
soundfile==0.12.1
librosa==0.10.1              # Audio resampling and processing
scipy==1.11.1                # Signal processing for audio

# FastAPI and web server (existing - keep)
fastapi==0.104.1
uvicorn[standard]==0.24.0
httpx==0.25.0
pydantic==2.4.2

# LiveKit integration (existing - keep)
livekit==0.11.1              # LiveKit Python SDK for real-time streaming

# PyTorch (minimal version for Kokoro)
torch==2.1.0                 # Kokoro uses PyTorch for voice pack loading
torchaudio==2.1.0            # Audio tensor operations

# Additional utilities
requests==2.31.0             # For downloading models and voice packs
pathos==0.3.1                # Async utilities if needed
tempfile                     # Built-in - for temporary file handling
hashlib                      # Built-in - for reference audio caching

# Optional optimizations
# accelerate==0.24.1         # HuggingFace optimizations (if needed)
# optimum[onnxruntime]==1.16.0  # ONNX optimizations

# Development and debugging
psutil==5.9.6                # System monitoring

# Note: Kokoro model files (~100MB total) will be auto-downloaded on first run
# Voice packs (~10MB each) will be cached locally
# Total disk space needed: ~200MB (much smaller than Chatterbox)