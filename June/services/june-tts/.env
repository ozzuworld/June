# --- OpenVoice V2 runtime ---
OPENVOICE_CHECKPOINTS_V2=/models/openvoice/checkpoints_v2
OPENVOICE_DEVICE=${OPENVOICE_DEVICE:-cuda}   # set to "cpu" if no GPU

# --- API binding (server listens here) ---
HOST=0.0.0.0
PORT=8000
WORKERS=1
CORS_ALLOW_ORIGINS=*

# --- Client target for curl/tools (what your shell will contact) ---
# If you're on the same machine, 127.0.0.1 is correct even if the server binds 0.0.0.0
HOST_FOR_CLIENT=127.0.0.1

# --- MeloTTS defaults ---
MELO_SPEAKER_ID=0
MELO_LANGUAGE=EN

# --- Limits ---
MAX_FILE_SIZE=$((20*1024*1024))  # 20MB
MAX_TEXT_LEN=2000
