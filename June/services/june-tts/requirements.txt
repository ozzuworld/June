# June TTS Service - Chatterbox TTS with optimized dependencies
# Core TTS Engine - Install from source to avoid version conflicts
# chatterbox-tts will be installed from source in Dockerfile

# FastAPI and web server
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Audio processing - compatible versions
soundfile==0.12.1
numpy>=1.24.0,<2.0  # Compatible with Chatterbox requirements
scipy==1.11.3

# LiveKit integration
livekit==0.11.1
livekit-api==0.11.1

# Networking and HTTP
httpx==0.27.2
websockets>=11.0.0

# Utilities
psutil>=5.9.0

# GPU monitoring (optional)
nvidia-ml-py>=11.5.0

# Development and debugging
rich>=13.0.0

# Chatterbox dependencies (will be installed manually in Dockerfile)
# librosa==0.10.1
# transformers==4.35.0
# diffusers
# resemble-perth
# conformer
# safetensors

# Note: PyTorch, Chatterbox TTS and its dependencies are installed 
# separately in Dockerfile to handle version conflicts properly