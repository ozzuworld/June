# June TTS Service - CosyVoice2 Dependencies
# TTS-only stack with CosyVoice2 engine

# Web Framework
fastapi==0.104.1
uvicorn==0.24.0
python-multipart
httpx==0.25.2

# LiveKit Integration
livekit==0.11.1

# Core ML/TTS dependencies
transformers>=4.30.0,<5.0.0

# Audio Processing
soundfile==0.12.1
scipy>=1.11.0,<2.0.0
librosa>=0.10.0,<1.0.0
numpy>=1.24.0,<2.0.0

# CosyVoice2 TTS Engine
# Install from GitHub since it's not on PyPI
# This will handle its own dependencies including any needed av/ffmpeg libs
git+https://github.com/FunAudioLLM/CosyVoice.git

# Additional dependencies that CosyVoice might need
# (these are commonly required by TTS models)
torchaudio  # Already installed via Dockerfile but listed for clarity
sentencepiece  # For tokenization if needed
pydub  # For audio format handling
matplotlib  # For any visualization in CosyVoice
