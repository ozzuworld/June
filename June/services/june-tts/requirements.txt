# June TTS Service - Kokoro (compatible versions)
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Kokoro with compatible torch stack
kokoro>=0.9.4
soundfile==0.12.1
numpy>=1.24.0,<2.0

# Audio processing minimal
scipy==1.11.3

# LiveKit and HTTP
livekit==0.11.1
httpx==0.27.2

# API validation
pydantic==2.5.0

# Compatible PyTorch stack (avoid torchvision conflicts)
torch==2.1.0
torchaudio==2.1.0
# Do not install torchvision (causes nms operator conflict)

# Transformers compatible version
transformers==4.35.0