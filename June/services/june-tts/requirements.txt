# XTTS v2 TTS Requirements (Coqui TTS)
# PyTorch (installed separately in Dockerfile for CUDA support)

# Coqui TTS with XTTS v2
coqui-tts>=0.22.0

# Audio processing
soundfile>=0.12.1
librosa>=0.10.1
scipy>=1.11.4
numpy<2.0.0

# DeepSpeed for faster inference (optional but recommended)
deepspeed==0.10.3

# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
httpx==0.25.0

# LiveKit support
livekit==0.11.1
protobuf>=4.21.0,<5.0.0

# Database and utilities
asyncpg
python-multipart

# Hugging Face
huggingface-hub[cli]

# Japanese language support (required for XTTS v2 Japanese synthesis)
cutlet>=0.3.0
mecab-python3>=1.0.6
unidic-lite>=1.0.8
