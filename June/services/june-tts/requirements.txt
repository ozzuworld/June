# File: June/services/june-tts/requirements.txt
# Service dependencies only (Chatterbox installed via git in Dockerfile)

# Core web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Authentication and HTTP
PyJWT[crypto]==2.8.0
cryptography==41.0.7
httpx==0.27.0

# File handling
python-multipart==0.0.9
aiofiles==23.2.0

# Note: Chatterbox TTS and its dependencies (PyTorch, torchaudio, etc.) 
# are installed directly from GitHub in the Dockerfile using:
# git clone https://github.com/resemble-ai/chatterbox.git && pip install -e .