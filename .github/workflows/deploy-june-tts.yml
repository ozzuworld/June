name: Deploy june-tts

on:
  push:
    paths:
      - 'services/june-tts/**'
      - 'k8s/june-tts-deployment.yaml'

env:
  REGISTRY: docker.io/${{ secrets.DOCKERHUB_USERNAME }}
  KUBE_NAMESPACE: june

permissions:
  contents: read

jobs:
  set-kubeconfig:
    runs-on: ubuntu-latest
    outputs:
      kubeconfig: ${{ steps.kc.outputs.kubeconfig }}
    steps:
      - name: Decode Kubeconfig
        id: kc
        run: |
          echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 --decode > kubeconfig
          chmod 600 kubeconfig

  build-and-deploy:
    needs: set-kubeconfig
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push june-tts Image
        run: |
          docker build -t $REGISTRY/june-tts:${{ github.sha }} services/june-tts
          docker push $REGISTRY/june-tts:${{ github.sha }}

      - name: Update Kubernetes Deployment
        env:
          KUBECONFIG: ${{ needs.set-kubeconfig.outputs.kubeconfig }}
        run: |
          kubectl set image deployment/june-tts \
            june-tts=$REGISTRY/june-tts:${{ github.sha }} \
            -n $KUBE_NAMESPACE
