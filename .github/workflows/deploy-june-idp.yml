name: Deploy june-idp

on:
  push:
    paths:
      - 'services/june-idp/**'
      - 'k8s/june-idp-deployment.yaml'

env:
  REGISTRY: docker.io/${{ secrets.DOCKERHUB_USERNAME }}
  KUBE_NAMESPACE: june

permissions:
  contents: read

jobs:
  set-kubeconfig:
    runs-on: ubuntu-latest
    outputs:
      kubeconfig: ${{ steps.kc.outputs.kubeconfig }}
    steps:
      - name: Decode Kubeconfig
        id: kc
        run: |
          echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 --decode > kubeconfig
          chmod 600 kubeconfig

  build-and-deploy:
    needs: set-kubeconfig
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push june-idp Image
        run: |
          docker build -t $REGISTRY/june-idp:${{ github.sha }} services/june-idp
          docker push $REGISTRY/june-idp:${{ github.sha }}

      - name: Update Kubernetes Deployment
        env:
          KUBECONFIG: ${{ needs.set-kubeconfig.outputs.kubeconfig }}
        run: |
          kubectl set image deployment/june-idp \
            june-idp=$REGISTRY/june-idp:${{ github.sha }} \
            -n $KUBE_NAMESPACE
