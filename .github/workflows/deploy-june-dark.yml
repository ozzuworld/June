name: Deploy june-dark

on:
  push:
    paths:
      - 'June/services/june-dark/**'
      - 'k8s/june-dark-deployment.yaml'

env:
  REGISTRY: docker.io/${{ secrets.DOCKERHUB_USERNAME }}
  KUBE_NAMESPACE: june

permissions:
  contents: read

jobs:
  set-kubeconfig:
    runs-on: ubuntu-latest
    outputs:
      kubeconfig: ${{ steps.kc.outputs.kubeconfig }}
    steps:
      - name: Decode Kubeconfig
        id: kc
        run: |
          echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 --decode > kubeconfig
          chmod 600 kubeconfig

  build-and-deploy:
    needs: set-kubeconfig
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push june-dark Image
        run: |
          docker build -t $REGISTRY/june-dark:${{ github.sha }} services/june-dark
          docker push $REGISTRY/june-dark:${{ github.sha }}

      - name: Update Kubernetes Deployment
        env:
          KUBECONFIG: ${{ needs.set-kubeconfig.outputs.kubeconfig }}
        run: |
          kubectl set image deployment/june-dark \
            june-dark=$REGISTRY/june-dark:${{ github.sha }} \
            -n $KUBE_NAMESPACE
