# Tailscale OAuth Secret Example
# 
# 1. Get OAuth credentials from: https://login.tailscale.com/admin/settings/oauth
# 2. Create OAuth client with scopes: "Devices" (Write)
# 3. Copy this file: cp tailscale-secret.yaml.example tailscale-secret.yaml
# 4. Replace CLIENT_ID and CLIENT_SECRET with your actual values
# 5. Apply: kubectl apply -f tailscale-secret.yaml
#
# IMPORTANT: Add tailscale-secret.yaml to .gitignore to avoid committing secrets

apiVersion: v1
kind: Secret
metadata:
  name: operator-oauth
  namespace: tailscale
type: Opaque
stringData:
  client_id: "YOUR_TAILSCALE_CLIENT_ID"
  client_secret: "YOUR_TAILSCALE_CLIENT_SECRET"