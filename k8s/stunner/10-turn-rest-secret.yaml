
apiVersion: v1
kind: Secret
metadata:
  name: stunner-auth
  namespace: stunner
type: Opaque
stringData:
  # Shared HMAC secret for time-limited TURN REST credentials
  static-auth-secret: "CHANGE_ME_TO_A_LONG_RANDOM_BASE64_STRING"
