# STUNner Authentication Secret Template
# This file is processed by install-livekit.sh to replace placeholders
apiVersion: v1
kind: Secret
metadata:
  name: stunner-auth-secret
  namespace: stunner
type: Opaque
stringData:
  type: static
  username: june-user
  password: Pokemon123!

# Note: Credentials will be replaced during installation
# The install-livekit.sh script uses sed to replace placeholders
# Production deployments should use more secure credentials