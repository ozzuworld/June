# TEMPLATE: don't commit real passwords. Create the Secret at deploy time.
apiVersion: v1
kind: Secret
metadata:
  name: stunner-auth-secret
  namespace: stunner
type: Opaque
stringData:
  type: static
  username: demo
  password: supersecret


###### CREATE IN K8S

##kubectl -n stunner create secret generic stunner-auth-secret \
##  --from-literal=type=static \
##  --from-literal=username="$TURN_USERNAME" \
##  --from-literal=password="$TURN_PASSWORD" \
##  --dry-run=client -o yaml | kubectl apply -f -
