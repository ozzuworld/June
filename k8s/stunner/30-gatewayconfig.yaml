apiVersion: stunner.l7mp.io/v1
kind: GatewayConfig
metadata:
  name: stunner-gatewayconfig
  namespace: stunner
spec:
  realm: turn.example.com
  authRef:
    name: stunner-auth-secret
    namespace: stunner
  dataplane: hostnet-ds
