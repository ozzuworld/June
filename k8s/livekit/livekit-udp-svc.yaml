apiVersion: v1
kind: Service
metadata:
  name: livekit-udp
  namespace: media
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: livekit
    app.kubernetes.io/name: livekit-server
  ports:
    - name: rtp-udp
      protocol: UDP
      port: 7882
      targetPort: 7882
