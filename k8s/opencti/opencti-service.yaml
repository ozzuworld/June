apiVersion: v1
kind: Service
metadata:
  name: opencti-server
  namespace: opencti
  labels:
    app.kubernetes.io/name: opencti
    app.kubernetes.io/component: server
spec:
  selector:
    app.kubernetes.io/name: opencti
    app.kubernetes.io/component: server
  ports:
  - port: 4000
    targetPort: 4000
    protocol: TCP
  type: ClusterIP