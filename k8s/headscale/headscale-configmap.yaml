# Patch: add IP prefixes required by Headscale
apiVersion: v1
kind: ConfigMap
metadata:
  name: headscale-config
  namespace: headscale
data:
  config.yaml: |
    server_url: https://headscale.ozzu.world
    listen_addr: 0.0.0.0:50443
    metrics_listen_addr: 0.0.0.0:50480
    disable_check_updates: true

    db_type: sqlite
    db_path: /var/lib/headscale/db.sqlite

    noise:
      private_key_path: /var/lib/headscale/noise_private.key

    # Assign address space to your tailnet
    ip_prefixes:
      - 100.64.0.0/10     # CGNAT range (IPv4)
      - fd7a:115c:a1e0::/48  # Unique local IPv6 prefix (example)

    dns:
      magic_dns: true
      base_domain: tail.ozzu.world
      override_local_dns: false
      nameservers:
        global: ["1.1.1.1", "8.8.8.8"]

    derp:
      urls: ["https://controlplane.tailscale.com/derpmap/default"]
      auto_update_enabled: true
      update_frequency: 24h
