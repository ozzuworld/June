# k8s/june-services/june-orchestrator-optimized.yaml
# OPTIMIZED: Single replica with proper resource allocation

apiVersion: apps/v1
kind: Deployment
metadata:
  name: june-orchestrator
  namespace: june-services
  labels:
    app: june-orchestrator
    component: orchestrator
spec:
  # FIXED: Single replica only
  replicas: 1
  selector:
    matchLabels:
      app: june-orchestrator
  template:
    metadata:
      labels:
        app: june-orchestrator
        component: orchestrator
    spec:
      serviceAccountName: june-orchestrator
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      
      containers:
      - name: june-orchestrator
        image: us-central1-docker.pkg.dev/main-buffer-469817-v7/june/june-orchestrator:latest
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        
        env:
        # Basic configuration
        - name: PORT
          value: "8080"
        - name: LOG_LEVEL
          value: "INFO"
        
        # Service URLs (internal cluster communication)
        - name: STT_SERVICE_URL
          value: "http://june-stt:8080"
        - name: TTS_SERVICE_URL
          value: "http://june-tts:8080"
        - name: KC_BASE_URL
          value: "http://june-idp:8080"
        - name: KC_REALM
          value: "june"
        
        # External APIs
        - name: GEMINI_API_KEY
          valueFrom:
            secretKeyRef:
              name: june-secrets
              key: GEMINI_API_KEY
        - name: CHATTERBOX_API_KEY
          valueFrom:
            secretKeyRef:
              name: june-secrets
              key: CHATTERBOX_API_KEY
        
        # Service authentication
        - name: ORCHESTRATOR_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: june-secrets
              key: ORCHESTRATOR_CLIENT_ID
        - name: ORCHESTRATOR_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: june-secrets
              key: ORCHESTRATOR_CLIENT_SECRET
        
        # OPTIMIZED: Proper resources for single pod
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        
        # Health checks
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        
        startupProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 30

---
apiVersion: v1
kind: Service
metadata:
  name: june-orchestrator
  namespace: june-services
  labels:
    app: june-orchestrator
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: june-orchestrator

---
# june-stt-optimized.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: june-stt
  namespace: june-services
  labels:
    app: june-stt
    component: stt
spec:
  # FIXED: Single replica only
  replicas: 1
  selector:
    matchLabels:
      app: june-stt
  template:
    metadata:
      labels:
        app: june-stt
        component: stt
    spec:
      serviceAccountName: june-stt
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      
      containers:
      - name: june-stt
        image: us-central1-docker.pkg.dev/main-buffer-469817-v7/june/june-stt:latest
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        
        env:
        - name: PORT
          value: "8080"
        - name: LOG_LEVEL
          value: "INFO"
        - name: KC_BASE_URL
          value: "http://june-idp:8080"
        - name: KC_REALM
          value: "june"
        
        # Service authentication
        - name: STT_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: june-secrets
              key: STT_CLIENT_ID
        - name: STT_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: june-secrets
              key: STT_CLIENT_SECRET
        
        # OPTIMIZED: Higher resources for STT processing
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
        
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: june-stt
  namespace: june-services
  labels:
    app: june-stt
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: june-stt

---
# june-tts-optimized.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: june-tts
  namespace: june-services
  labels:
    app: june-tts
    component: tts
spec:
  # FIXED: Single replica only
  replicas: 1
  selector:
    matchLabels:
      app: june-tts
  template:
    metadata:
      labels:
        app: june-tts
        component: tts
    spec:
      serviceAccountName: june-tts
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      
      containers:
      - name: june-tts
        image: us-central1-docker.pkg.dev/main-buffer-469817-v7/june/june-tts:latest
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        
        env:
        - name: PORT
          value: "8080"
        - name: LOG_LEVEL
          value: "INFO"
        - name: KC_BASE_URL
          value: "http://june-idp:8080"
        - name: KC_REALM
          value: "june"
        - name: CHATTERBOX_API_URL
          value: "https://api.chatterboxtts.com"
        
        # Service authentication
        - name: TTS_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: june-secrets
              key: TTS_CLIENT_ID
        - name: TTS_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: june-secrets
              key: TTS_CLIENT_SECRET
        - name: CHATTERBOX_API_KEY
          valueFrom:
            secretKeyRef:
              name: june-secrets
              key: CHATTERBOX_API_KEY
        
        # OPTIMIZED: Moderate resources for TTS proxy
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: june-tts
  namespace: june-services
  labels:
    app: june-tts
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: june-tts

---
# Service Accounts
apiVersion: v1
kind: ServiceAccount
metadata:
  name: june-orchestrator
  namespace: june-services
  labels:
    app: june-orchestrator
  annotations:
    iam.gke.io/gcp-service-account: june-orchestrator-gke@main-buffer-469817-v7.iam.gserviceaccount.com

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: june-stt
  namespace: june-services
  labels:
    app: june-stt
  annotations:
    iam.gke.io/gcp-service-account: june-stt-gke@main-buffer-469817-v7.iam.gserviceaccount.com

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: june-tts
  namespace: june-services
  labels:
    app: june-tts
  annotations:
    iam.gke.io/gcp-service-account: june-tts-gke@main-buffer-469817-v7.iam.gserviceaccount.com