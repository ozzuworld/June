# June Platform Config

# Redis configuration for June Orchestrator context memory
REDIS_HOST=june-redis-master.june-services.svc.cluster.local
REDIS_PORT=6379
REDIS_DB=1
# Optional: Set strong password for production
REDIS_PASSWORD=

# Domain and certificates
DOMAIN=ozzu.world
LETSENCRYPT_EMAIL=admin@ozzu.world

# API tokens
GEMINI_API_KEY=changeme
CLOUDFLARE_TOKEN=changeme

# Database & identity
POSTGRESQL_PASSWORD=Pokemon123!
KEYCLOAK_ADMIN_PASSWORD=Pokemon123!

# STUN/TURN
TURN_USERNAME=june-user
STUNNER_PASSWORD=Pokemon123!

# GPU settings
# Number of time-sliced logical GPUs per physical GPU (via NVIDIA GPU Operator config)
# Set to 1 to disable time-slicing. Default 2 slices per GPU.
GPU_TIMESLICING_REPLICAS=2
