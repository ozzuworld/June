# June Platform Config

# Domain and certificates
DOMAIN=ozzu.world
LETSENCRYPT_EMAIL=admin@ozzu.world

# API tokens
GEMINI_API_KEY=changeme
CLOUDFLARE_TOKEN=changeme

# Database & identity
POSTGRESQL_PASSWORD=Pokemon123!
KEYCLOAK_ADMIN_PASSWORD=Pokemon123!

# STUN/TURN
TURN_USERNAME=june-user
STUNNER_PASSWORD=Pokemon123!

# GPU settings
# Number of time-sliced logical GPUs per physical GPU (via NVIDIA GPU Operator config)
# Set to 1 to disable time-slicing. Default 2 slices per GPU.
GPU_TIMESLICING_REPLICAS=2

# Vast.ai Remote GPU Provider (Optional)
# Get your API key from: https://console.vast.ai/ -> Account -> API Keys
# VAST_API_KEY=vast_api_key_your_key_here

# Vast.ai Instance Selection (Optional - defaults shown)
# VAST_GPU_TYPE=RTX3060
# VAST_MAX_PRICE_PER_HOUR=0.50
# VAST_MIN_GPU_MEMORY=12
# VAST_RELIABILITY_SCORE=0.95
# VAST_MIN_DOWNLOAD_SPEED=100
# VAST_MIN_UPLOAD_SPEED=100
# VAST_DATACENTER_LOCATION=US
# VAST_PREFERRED_REGIONS=US-CA,US-TX,US-NY,US
# VAST_VERIFIED_ONLY=true
# VAST_RENTABLE_ONLY=true