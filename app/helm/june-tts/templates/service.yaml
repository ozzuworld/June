apiVersion: v1
kind: Service
metadata:
  name: {{ include "june-tts.fullname" . }}
  labels: {{- include "june-tts.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector: {{- include "june-tts.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
