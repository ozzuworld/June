apiVersion: v1
kind: Service
metadata:
  name: {{ include "june-orchestrator.fullname" . }}
  labels: {{- include "june-orchestrator.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector: {{- include "june-orchestrator.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
